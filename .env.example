# Server
PORT=5050
CLIENT_ORIGIN=http://localhost:5173
MAX_FILE_SIZE_MB=25

# Optional: enables real AI generation. Without this key, the app runs in safe demo mode.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
